{
  "name": "better-gmail",
  "version": "0.1.0",
  "private": true,
  "description": "Better Gmail — a Chrome extension that gives Gmail + Google Calendar Outlook-style UX, built for environments with NO Gmail API access (no OAuth, no Cloud project). Everything works purely in the page as a content script.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  }
}
